<%= form_with(model: course) do |form| %>
  <% if course.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
        <% course.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :ects, style: "display: block" %>
    <%= form.number_field :ects %>
  </div>

  <div>
    <%= form.label :egzam, style: "display: block" %>
    <%= form.check_box :egzam %>
  </div>

  <div>
    <%= form.label :field_id, style: "display: block" %>
    <%= form.collection_select :field_id, Field.all, :id, :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
